<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coders.laundry.repository.HomeRepository">
    <select id="selectAllTeamMemberList" resultType="MemberEntity">
        SELECT member_id, phone_num, password, nickname,
               birthday, gender, auto_login_yn, join_date
          FROM member;
    </select>

    <insert
      id="create"
      parameterType="MemberEntity"
      useGeneratedKeys="true"
      keyProperty="memberId"
      keyColumn="member_id">
        INSERT INTO member (phone_num, password, nickname, birthday, gender)
        VALUES (#{phone_num}, #{password}, #{nickname}, #{birthday}, #{gender});
    </insert>
</mapper>
